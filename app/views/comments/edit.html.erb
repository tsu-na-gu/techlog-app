<main>
  <div class="mx-auto max-w-3xl px-5 py-16 sm:px-6 sm:plg:px-8">
    <%= link_to sanitize("&larr; 投稿一覧に戻る"), posts_path %>
    <div>
      <h2 class="text-2xl font-medium text-black">コメントを編集</h2>
    </div>

    <%= turbo_frame_tag @comment do %>
      <%= render 'form', post: @post, comment: @comment %>
    <% end %>
  </div>
</main>