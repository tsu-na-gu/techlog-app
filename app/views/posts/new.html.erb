<div class="mx-auto max-w-3xl px-5 py-16 sm:px-6 sm:plg:px-8">
  <div class="flex flex-row justify-between">
    <h2 class="text-xl font-medium text-black">新規投稿</h2>
  </div>

  <%= turbo_frame_tag @post do %>
    <%= render 'form', post: @post %>
  <% end %>
</div>